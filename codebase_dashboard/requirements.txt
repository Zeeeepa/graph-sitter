# Reflex Dashboard Requirements
reflex>=0.4.0
httpx>=0.25.0
pydantic>=2.0.0

# Backend API Requirements (for development)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Graph-sitter integration (already available in the repository)
# graph-sitter (from local src/)
